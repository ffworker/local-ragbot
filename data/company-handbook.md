# Demo Knowledge Base

This bot is only allowed to answer questions using local data that was indexed
from the `data/` directory.

If the answer is not present in local data, the bot must say that it does not
know from the available local documents.

The first milestone is a text-only RAG chatbot. Voice input and output will be
added later after retrieval quality is stable.

